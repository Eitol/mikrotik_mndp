name: mikrotik_mndp
description: "Mikrotik MNDP Discovery Protocol"
version: 0.0.2
homepage: https://github.com/Eitol/mikrotik_mndp
repository: https://github.com/Eitol/mikrotik_mndp
environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  network_info_plus: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  assets:
    - assets/products.json

  plugin:
    platforms:
      android:
        package: io.flutter.plugins.firebase.crashlytics
        pluginClass: FlutterFirebaseCrashlyticsPlugin
